#! /usr/bin/ruby

# parse options and command for this ameem
dir = File.expand_path(File.dirname(__FILE__)+'/../lib')
$LOAD_PATH << dir unless $LOAD_PATH.include?(dir)
require 'provenance'

begin
  prov=Provenance.new(ARGV)
  prov.exec
  prov.clean
#rescue RuntimeError => error
#  print error.message+"\n"
#  provenance "--help"
end

